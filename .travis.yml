sudo: required

env:
  - VERSION=2.3.0 TAG=latest
  
services:
   - docker

script:
   - ./build build

after_success:
   - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
   - ./build deploy
